# Railway config-as-code for the DAM worker service (deploy/dam/README.md).
[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/dam/Dockerfile"

[deploy]
startCommand = "sh deploy/dam/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
