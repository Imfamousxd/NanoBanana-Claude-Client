# Claude Code entry point

@AGENTS.md

Use the shared agent contract above. Claude-specific project permissions belong in
`.claude/settings.local.json`; brand facts and prompt rules do not. Do not copy the shared contract
back into this file, because duplicated instructions drift and consume context.
